#include <bits/stdc++.h>
#define N 10005
using namespace std;
vector<string> vs[N];
int main()  {
    int n,t;
    scanf("%d%d",&t,&n);
    while (t--) {
        for (int i=0;i<n;i++)
    }
    return 0;
}
